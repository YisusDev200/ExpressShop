const blogValidators = require("./blog.validator");
const commonValidators = require("./common.validator");
const couponValidators = require("./coupon.validator");
const enquiryValidators = require("./enquiry.validator");
const productValidators = require("./product.validator");
const userValidators = require("./user.validator");

module.exports = {
  blogValidators,
  commonValidators,
  couponValidators,
  enquiryValidators,
  productValidators,
  userValidators,
};
